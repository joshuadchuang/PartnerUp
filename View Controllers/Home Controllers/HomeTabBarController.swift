//
//  HomeTabBarController.swift
//  PartnerUp
//
//  Created by Joshua Chuang on 1/21/21.
//

import UIKit

class HomeTabBarController: UITabBarController {
    
    // MARK: This class acts as the default navigation controller
    // The reason why we don't have one of the pages (Home, Conversations, Profile) acts as the default page
    // is because those may be taken out or changed, but this navigation controller will always remain
    
    // MARK: Default Override Function
    override func viewDidLoad() {
        super.viewDidLoad()
    }

}
